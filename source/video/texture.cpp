#include "video/texture.h"

namespace TURBO
{
    namespace VIDEO
    {
//        Texture::Texture(Renderer &renderer, Uint32 format, Sint32 access, Sint32 w, Sint32 h)
//            : SDL2pp::Texture(renderer, format, -1, w, h), renderer(renderer)
//        {
//
//        }
    }
}